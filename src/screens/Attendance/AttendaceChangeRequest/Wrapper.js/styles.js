const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
    container: {
        marginHorizontal: 16,
        // borderWidth: 1,
        flex: 1
    }
})

export default styles