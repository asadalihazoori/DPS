import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    mainView: {
        marginHorizontal: 16,
        // borderWidth: 1,
        marginTop: 24,
    },

    imageView: {
        alignItems: 'center',
        // borderWidth: 1,
    },

    cameraIconView: {
        position: 'absolute',
        bottom: 0,
        right: 0
    },

    featuresView: {
        marginTop: 32,
        // borderWidth: 1
    }

})